export enum SectionRoutes {
  Hero = 'hero',
  Experience = 'experience',
  Projects = 'projects',
  Contact = 'contact',
}

export enum AppRoutes {
  Home = '/',
  Resume = `/resume`,
  NotFound = '*',
}
